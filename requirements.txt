anthropic
flask
python-dotenv
pandas
numpy
requests
yfinance
pytz

# Camelot shared engine (Step 2 -- pinned). Bumped v1.1.2 -> v1.1.5 (21 Aug 2026): v1.1.2 predates the
# external-close/CSV-header (v1.1.3), orphan-adoption (v1.1.4) and reconcile_orders (v1.1.5) fixes -- a fresh
# install must not silently pull the stale engine. Dell runs the editable install; this pin governs fresh installs (K1 promotion).
camelot-engine @ git+https://github.com/nicksapsford/CamelotEngineAI.git@v1.1.5
