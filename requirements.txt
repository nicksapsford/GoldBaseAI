anthropic
flask
python-dotenv
pandas
numpy
requests
yfinance
pytz

# Camelot shared engine (Step 2 -- pinned). Bumped to v1.1.6 (21 Aug 2026): adds the opt-in CAMELOT_FORCE_DEMO
# structural DEMO lock the K1 test instance needs (unset -> byte-identical to v1.1.5). Predecessors since v1.1.2:
# v1.1.3 external-close/CSV-header, v1.1.4 orphan-adoption, v1.1.5 reconcile_orders. Tag + pin bumped together in
# the same push (standing rule: a stale pin silently pulls a stale engine). Dell runs the editable install; this
# pin governs FRESH installs (the path K1 uses).
camelot-engine @ git+https://github.com/nicksapsford/CamelotEngineAI.git@v1.1.6
